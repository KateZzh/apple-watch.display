// const time = new Date();
const time = new Date(2013, 0, 1, 0, 0, 0, 0);

const dynamicTime = document.querySelector(".wrapper h1");
const dynamicYear = document.querySelector(".wrapper p");

// function doDynamicTime(time_, dynamicTime_) {
//   dynamicTime_.innerHTML = ;
// }
// setInterval(doDynamicTime, 1000, time, dynamicTime);

// function doDynamicYear(time_, dynamicYear_) {
//   dynamicYear_.innerHTML = time_.getFullYear();
// }
// setInterval(doDynamicYear, 1000, time, dynamicYear);
